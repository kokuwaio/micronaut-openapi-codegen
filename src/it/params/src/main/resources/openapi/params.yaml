openapi: 3.0.2
info:
  title: Test Spec
  version: '0.6'
servers:
- url: http://localhost:8080
paths:
  /query:
    get:
      description: 'Get'
      operationId: getMultipleLists
      parameters:
      - name: a
        in: query
        required: false
        schema:
          type: array
          items:
            type: string
      - name: b
        in: query
        required: false
        schema:
          type: array
          items:
            type: string
      tags:
      - query
      responses:
        200:
          description: Ok.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Model'
components:
  schemas:
    Model:
      type: object
      properties:
        a:
          type: array
          items:
            type: string
        b:
          type: array
          items:
            type: string
