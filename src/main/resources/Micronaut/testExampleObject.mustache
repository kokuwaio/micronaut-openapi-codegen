package {{package}};

{{#models}}
import {{importPath}}.*;

public class {{classname}}TestExample {

        {{#model}}
        {{#isEnum}}
        public static {{classname}} INSTANCE = {{classname}}.values()[0];
        {{/isEnum}}
        {{^isEnum}}
        {{^discriminator}}
        public static {{classname}} INSTANCE = new {{classname}}();
        {
            //initialize fields
            {{#vars}}
                {{#isEnum}}
                INSTANCE.set{{nameInCamelCase}}({{datatypeWithEnum}}.toEnum({{{example}}}));
                {{/isEnum}}
                {{#isModel}}
                INSTANCE.set{{nameInCamelCase}}({{datatypeWithEnum}}TestExample.INSTANCE);
                {{/isModel}}
                {{^isEnum}}
                {{^isModel}}
                INSTANCE.set{{nameInCamelCase}}({{>testExampleObjectPropertyExample}});
                {{/isModel}}
                {{/isEnum}}
            {{/vars}}
        }
        {{/discriminator}}
        {{/isEnum}}
        {{/model}}
}
{{/models}}
