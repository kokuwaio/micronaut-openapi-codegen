openapi: 3.0.3
info:
  title: Spec for <https://github.com/kokuwaio/micronaut-openapi-codegen/issues/414>
  version: "1"
paths:
  /doIt:
    get:
      operationId: doIt
      responses:
        200:
          description: OK
components:
  schemas:
    ModelObject:
      type: object
      properties:
        a:
          type: string
    ModelEnum:
      type: string
      enum: [foo, bar]
